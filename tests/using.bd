create funct using(file){
	if (`@{file} == 'recimp'`){
		using('ri.bd')
	}-
	else{
	pyparse `try:
	parse(open('@{file}').read())
except FileNotFoundError:
	parse(open('Bird/lib/@{file}').read())`
	}
}[global=&sep=-];
create funct test(a){
	return 'yay'
}[global=];
using('recimp');
writeout(test('a'));
writeout(test('a'))